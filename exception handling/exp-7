#include <iostream>

int main() {
    try {
        throw std::string("An error occurred");
    } catch (const std::string& e) {
        std::cout << "Caught exception: " << e << std::endl;
    }
    return 0;
}
